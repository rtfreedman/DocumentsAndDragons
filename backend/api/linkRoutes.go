package api

import "github.com/gorilla/mux"

// LinkRoutes links the routes to the mux router
func LinkRoutes(r *mux.Router) {
	// r.HandleFunc("/api/items/weapons/{weaponName}", getWeaponInformation).Methods("GET")
	return
}
